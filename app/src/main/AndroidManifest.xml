<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.sadsa">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Sadsa">
        <activity android:name=".cardview"></activity>
        <activity android:name=".RecyclerView" />
        <activity android:name=".recycler" />
        <activity android:name=".List" />
        <activity android:name=".Phonenumber" />
        <activity
            android:name=".other2"
            android:label="@string/title_activity_other2"
            android:theme="@style/Theme.Sadsa.NoActionBar" />
        <activity
            android:name=".phone"
            android:label="@string/title_activity_phone"
            android:theme="@style/Theme.Sadsa.NoActionBar" />
        <activity android:name=".othermain" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>